process ASCIIWRITER =  {
#keep the logging output to a nice level
untracked PSet maxEvents = {untracked int32 input = 1}
source = PoolSource {

        untracked vstring fileNames ={
        "file:/tmp/kodolova/022D6BD0-AE49-DC11-A705-001617C3B78C.root"
	                              }
}

service = Tracer { untracked string indention = "$$"}
  
module asciiwriter = HcalConstantsASCIIWriter {
  untracked string fileInput = "hcalinput"	
  untracked string fileOutput = "hcaloutput"
}

path p = {asciiwriter}

}
